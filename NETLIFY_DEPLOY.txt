章鱼机 V2.10-R2.1 紧急修复 Netlify 直接部署版

方法一：
1. 登录 Netlify。
2. 打开 Deploys。
3. 将本 ZIP 直接拖入部署区域。

方法二：
1. 解压本 ZIP。
2. 将解压后的全部文件拖入 Netlify Deploys。

部署要求：
- 不需要 npm install。
- 不需要构建命令。
- Publish directory 为根目录“.”。
- index.html 必须位于部署根目录。

首次升级后若仍加载旧页面，请在浏览器中清除该站点缓存和 Service Worker 后刷新。
