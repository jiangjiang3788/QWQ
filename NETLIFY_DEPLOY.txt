章鱼机 V2.12-R2 悬浮球主操作收敛 Netlify 直接部署版

部署方式：
1. 解压本压缩包，将根目录整体上传到 Netlify。
2. 根目录包含 index.html、netlify.toml、_headers 与 _redirects。
3. 本版将模型切换、操作、详情、Proment、日志、功能覆盖和 Git 操作统一放到悬浮球页面顶部。
4. 记忆数据协议、备份格式和模型请求协议保持不变。
