V16.1 本地脚本修复

问题：页面启动依赖多个公共 CDN。CDN 被拦截、超时或不可达时，Dexie、DOMPurify 等全局对象不存在，数据库和功能模块会连锁加载失败。

修复：
1. 将 8 个第三方脚本放入 vendor/。
2. index.html 的第三方 script src 全部改为本地相对路径。
3. 在线图片、Google 字体、AI API、天气、音乐等网络功能保持原样。

部署：请上传整个目录，不能只上传 index.html；vendor/ 目录不可删除。
