QWQ 动态记忆 V5.6.6 完整代码包
STORE_VERSION=3
重点：收藏与结构化记忆按同类二级分组；PROMENT 私聊历史不设人工最大值；长条目只使用页面主滚动区；收藏计数与实际明细完整对账。

V5.6.6：
- 收藏一级标题下只保留“用户收藏”“角色收藏”两个二级分组，组内记录直接平铺；结构化记忆按表名分组，记录不再一条一个折叠框。
- 移除 PROMENT 聊天条数 max=200 与编译器 500 条硬上限；输入 0 表示发送全部可用私聊历史，正整数按实际消息条数截取。
- 私聊请求生成阶段不再先被角色 maxMemory 二次截断，PROMENT 成为私聊文本请求的唯一历史窗口控制。
- 修复操作运行时对所有数组统一 slice(0,120) 导致的收藏明细缺失；最新请求的 contextManifest、请求消息数组和来源 items 均完整保留。
- 全屏操作中心改成单一滚动容器，取消标题区 sticky 叠层与来源正文内部滚动，长内容展开后不会覆盖后续标题。
