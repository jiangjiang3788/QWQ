QWQ 动态记忆 V5.8.0 第一版完整代码包
STORE_VERSION=3

本次结果：
- 默认私聊提示词改为按角色扮演语义项目真实分段，并按最终发送顺序展示。
- 核心档案仍存储在记忆表中，但发送时作为身份锚点前置；字段按“角色档案 / 用户档案 / 双方关系 / 其他分类”分组渲染。
- {{user}} 与 {{char}} 作为存储占位符，仅在最终发送时替换为当前用户称呼和角色名，不改写原始数据。
- 世界书保留身份前、身份后、场景后置三个真实插入位置；预算选择与插入位置分离，条目只整条发送。
- 长期关系记忆与当前相关记忆分开；收藏、待办和其他相关表仍按现有召回规则进入当前相关层。
- 悬浮球和 Token 分布均按实际项目顺序读取最终请求，不再常态生成巨大的“核心系统规则”；异常残余才显示“未归类内容”。
- 第一版完整覆盖当前备份所使用的默认私聊路径；自定义 Prompt 与剧情节点保留原组装结构，但已支持 {{user}} / {{char}} 双占位符。
