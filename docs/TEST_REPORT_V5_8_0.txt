QWQ V5.8.0 第一版测试报告
生成日期：2026-07-30

一、语法检查
- 对 js/ 下全部 JavaScript 文件执行 node --check。
- 结果：全部通过。

二、自动测试
- 测试文件总数：21。
- 正常通过：20。
- 明确跳过：1（旧 KV 迁移测试缺少其专用外部 fixture，按测试原逻辑跳过）。
- 失败：0。

三、V5.8.0 专项覆盖
1. 核心档案按“角色档案、用户档案、双方关系”字段分类顺序输出。
2. 核心档案存在时，旧 persona / myPersona 不重复发送。
3. {{user}} / {{char}} 只在最终提示词阶段替换。
4. 世界书预算选择与插入位置分离。
5. 世界书条目完整发送，预算不足时整条排除。
6. 世界书 before / middle / after 对应身份前 / 身份后 / 场景后置。
7. 收藏记忆进入当前与相关记忆，不混入长期关系记忆。
8. 项目标签按实际发送顺序组装。
9. 悬浮球来源清单按项目精确提取。
10. 正常请求不生成 system.core_rules 或 system.unclassified。
11. 旧未分段请求仍以“未归类内容（需要检查）”进行兼容诊断。
12. Proment 关闭结构化记忆时会同时移除新项目式记忆标签。

四、实际备份只读验证
- 成功读取实际角色记忆表和世界书数据。
- 核心档案识别为 3 个字段分类：角色档案、用户档案、双方关系。
- 最终提示词中 {{user}} / {{char}} 均无残留。
- 核心档案存在时，旧人设未作为独立 char_settings 重复发送。
- 最终来源清单无未注册来源、无未归类系统正文，coverage.complete=true。
- 备份数据未写入代码包。

五、第一版边界
- 当前实际使用的默认私聊路径：完成项目式重构。
- 自定义 Prompt 与剧情节点：已支持 {{user}} / {{char}} 双占位符，仍保留旧的整体组装结构。
- 通话、日记、小剧场等独立模型任务：本版不改其任务提示词结构。
