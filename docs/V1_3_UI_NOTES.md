# V1.3 界面修订

## 已修复

- 记忆主页面可以纵向滚动。
- 新建表、编辑表、编辑记录、记忆设置等表单弹窗均可触摸滚动。
- 表名旁显示小绿点，表示该表已有正式写入或更新。
- KV字段名、Rows列标题旁显示小绿点，表示该字段已有正式写入或更新。
- 原有“用户明确/AI判断 · 新增/更新”文字标识继续保留。

## 绿点含义

绿点不是未读消息，也不会自动消失。它表示对应表或字段已经存在正式写入记录，便于快速定位哪些位置实际被记忆系统使用过。
