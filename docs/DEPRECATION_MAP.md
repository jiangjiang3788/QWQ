# 废弃与兼容清单

## R0 已从生产 UI 移除

- 电话空页面及其底部导航入口。
- 空 `storage-screen`。
- 隐藏的群聊创建按钮。
- 旧版本更新弹窗。

## 暂时保留兼容代码

- 群聊设置 DOM 与历史数据分支。
- 旧日记模式。
- 思维链、正则、状态栏等高级工具。
- 电量互动、保活和悬浮工具。
- `window.MemoryTable*` 全局接口。

## 删除流程

1. 先通过 Feature Flag 关闭入口。
2. 保留至少一个版本的数据导出能力。
3. 记录真实调用与用户数据情况。
4. 确认无依赖后删除运行代码。
5. 运行全部回归和模板数据对比。

## V2.9-R3

| 旧入口 | 当前行为 | 后续计划 |
|---|---|---|
| `more-screen` | 自动转到设置 App | R4 更新旧模块返回路径后删除旧 UI |
| API 单一长页 | 由四个工作区分组显示 | 保留原表单，逐步拆分组件 |

## V2.9-R6 入口收敛

| 旧入口/行为 | 新入口/行为 | 状态 |
|---|---|---|
| 桌面网格与 Dock 同时出现角色、记忆 | 角色、记忆只保留在 Dock | 已收敛 |
| Dock 直接放 API、数据、外观 | 统一进入设置 App | 已收敛 |
| 联系人作为桌面 App | 联系人归入聊天底栏 | 已收敛 |
| 设置中重复“角色管理/角色记忆” | 角色操作回到角色 App 和记忆 App | 已移除 |
| 页面返回按钮写死目标 | `OvoNavigation.back()` 返回真实来源 | 已替换 |
| 记忆工作区直接写 `uiState.workspace` | `persistWorkspaceState()` 单一写入口 | 已替换 |
