QWQ V2.10-R3.3 聊天同请求记忆核验

核验对象：章鱼喷墨_备份数据_2026-07-21_清理优化版.ee（仅读取记忆配置与上下文，不输出任何 API 凭据）
核验方式：使用 R3.3 实际 chat_ai.js 的最终请求体防线函数，分别构造 OpenAI-compatible 与 Gemini Provider 请求体。
说明：本次没有调用外部模型 API；核验的是实际发送前的最终 payload 构造。

实际备份配置：
- 聊天消息：19133 条
- memoryMode：vector（向量仅作为补充层）
- 结构化档案 enabled：true
- 已绑定模板：1，模板库命中：1
- 当前结构化档案上下文：2880 字符
- 结构化档案数据：约 429849 个 JSON 字符
- 向量条目：0，向量上下文：0 字符
- 收藏日记：0

最终请求体核验：
- OpenAI-compatible：structuredArchiveSent=true，档案正文 2880 字符，guardApplied=true
- Gemini：structuredArchiveSent=true，档案正文 2880 字符，guardApplied=true
- OpenAI structured_archive_memory 标签数量：1
- Gemini structured_archive_memory 标签数量：1
- 主聊天代码段 OVOAIRequestRuntime.request 调用数量：1
- 最终记忆核验是否发生在网络调用之前：true

判断：
- 结构化档案与聊天历史位于同一个主模型请求中，不会为了注入档案额外发送一次网络请求。
- 当前角色虽选择 vector，但向量条目为 0；R3.3 仍发送 2880 字符结构化档案，向量为空不会再导致长期档案整体缺失。
- 最终 payload 已存在档案时不会重复注入；缺失时在实际请求体中补入一次。
- 悬浮球“本次聊天记忆核验”读取的是这份最终请求级审计，而不是设计预览。
