# V5.4.5 KV 单例表单修复

- KV 表不再把“标题/内容”多条记录伪装成表单。
- KV 表整张表只保留一条单例记录；自定义字段就是左侧表单项目。
- 旧版 KV 数据会自动迁移：每条旧记录的标题转为字段名，内容转为该字段值。
- KV 表公共字段仅作为系统元数据保存并自动隐藏。
- 编辑表格中的自定义字段与左侧表单项一一对应。
- AI 对 KV 表只更新唯一记录，不再为每个项目新增记录。
- 自定义字段“必填”设置继续由写入器强制执行。
