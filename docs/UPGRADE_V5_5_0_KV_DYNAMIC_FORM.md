# V5.5.0 KV 动态单例表单重构

- KV 表不再注入分类、标签、标题、内容、来源、时间等固定业务字段。
- KV 表字段完全由用户在字段设置中定义。
- 左侧展示、手动编辑、AI写入和上下文发送均使用同一份动态字段定义。
- 每张 KV 表最多保存一条单例记录。
- 旧版 title/content 多记录 KV 数据自动迁移为动态字段。
- Rows 表继续保留公共字段体系。
- KV 与 Rows 切换时执行结构标准化和记录迁移。
