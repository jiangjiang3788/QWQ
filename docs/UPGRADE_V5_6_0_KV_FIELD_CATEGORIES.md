# V5.6.0 KV字段分类与动态表单统一

- KV表不要求分类、标签、标题、内容、来源、时间等公共字段。
- Rows表继续强制使用公共字段。
- KV分类成为字段结构属性 `field.category`，不是记录公共字段。
- 字段编辑器支持为每个KV字段设置分类。
- KV显示按字段分类分组，分类内保持字段顺序。
- 旧KV多记录迁移时，原记录category迁移到字段category。
- 导入校验、标准化、AI提示、上下文和渲染使用同一字段模型。
